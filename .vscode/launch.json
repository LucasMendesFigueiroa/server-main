
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Debug Nest with VSCode",
      "request": "launch",
      "command": "npm run debug",
      "cwd": "${workspaceFolder}",
    }
  ]
}
